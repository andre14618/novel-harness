# Lane Queue

## Active
- (empty — integrity ladder L40-L72 at diminishing returns on the 3-novel validation panel; dominant blocker shifted to ch2 plan-check-exhausted on continuity + halluc-ungrounded)

## Next
- (DONE 2026-05-03 → 2026-05-04) Codex round-1 review of the 2026-05-03 collaborative-proposal-workflow bundle (commits acf67c2…2c61d59) returned 3 HIGH + 4 MEDIUM + 4 LOW. All HIGHs + all MEDIUMs + 2 LOWs shipped as fix commits b554e42 → 4fec19e. Threads `019df0e2` (Package A), `019df0e8` (Package B), `019df0ed` (Package C). All 3 remaining LOWs closed 2026-05-04 as defense-in-depth: race-window deterministic test (commit `6249ea7`, exp #427); schema-version drift guard test (commit `1993e17`, exp #428); programmer-bug failure-path test verified already covered by existing `planner-canon-proposals.test.ts:537` (Codex MEDIUM 1 test — no new commit).
- (DONE 2026-05-04) Codex round-2 review of the fix bundle (b554e42…4fec19e) returned 0 HIGH / 3 MEDIUM / 0 LOW. Transactional core verified sound (atomic batch, predicate regex, schema-v2 coexistence). All 3 MEDIUMs shipped: M3 CLI cmdGenerate honors persistenceError (a9757d4, exp #423); M2 single-resolve findProposal moved inside try (245b9c7, exp #424); M1 strict persisted-outline schema (d913fb4, exp #425). Phase 3-6 substantial lanes are now unblocked.
- Browser hand-test the Phase 2B canon-proposal review panel at `/app/canon-proposals/:novelId` (lane shipped 2026-05-03 browser-untested per CLAUDE.md UI rule). On a real novel: confirm filters work (source / chapter / plannerOnly), Approve commits the fact + removes the row, Reject removes it without committing, Generate-from-outline fires + summary line shows. Then declare full clean pass.
- (DONE 2026-05-04 browser-untested) UI bulk-action affordance — `Approve all (N)` / `Reject all (N)` buttons now appear above the table when proposals are listed. Acts on visible/filtered set; window.confirm(); soft-cap 200/call (warns + truncates with "re-run for the rest" hint); per-row summary banner; optimistic remove on full success, reload-to-authoritative on partial errors. Browser hand-test required for clearance.
- (DONE 2026-05-04 browser-untested) UI audit-history view — status tab strip (`pending` | `approved` | `rejected` | `modified` | `all`) renders above the existing filter row; click applies immediately and re-fetches via the `?status=` query param. Non-pending tabs hide bulk-action + per-row Approve/Reject buttons (read-only audit view); rows show a status badge + resolved-at timestamp + operatorNote in the Decision column. Clear button preserves the active status tab. Browser hand-test required for clearance.
- (DONE 2026-05-04 browser-untested) UI modify-with-edits surface — pending rows now show a Modify… button alongside Approve/Reject. Clicking expands the Proposed-fact column into an inline form: original text shown as ghost-quote above an editable textarea, plus confidence input (0-1) and free-form note. Save sends `status: "modified" + modifiedFact + operatorNote + expectedStatus: "pending"`; id, kind, source stay fixed (audit trail). Cancel collapses without writing. Only the row being edited is busy. Browser hand-test required for clearance.
- Phase 3 of `docs/designs/collaborative-proposal-workflow.md` — Artifact Patch Proposal Cards. **Commit 1 (envelope shape + additive adjuster wrap) DONE 2026-05-04. Commit 2 (per-patch resolve route + hash-precondition) DONE 2026-05-04. Commit 2.5 (UI consumer of resolve route) DONE 2026-05-04 browser-untested. Commit 3 (regenerate-on-stale UI affordance) DONE 2026-05-04 browser-untested. Commit 5a (bulk quick actions: approve-all-low-risk + reject-all) DONE 2026-05-04 browser-untested. Commit 4 (persistence: proposal_envelopes table + insert + list route) DONE 2026-05-04. Commit 4 follow-up A (resolve-route persistence wiring + 409 alreadyResolved) DONE 2026-05-04. Commit 4 follow-up B (parentEnvelopeId provenance on regen) DONE 2026-05-04 browser-untested. Commit 4 follow-up C (UI loads persisted pending envelopes on AdjustPanel mount) DONE 2026-05-04 browser-untested. Commit 4 follow-up D (audit-history view in AdjustPanel) DONE 2026-05-04 browser-untested.** Remaining sub-items: LLM-firing quick actions: ask-alternatives + explain-patch (commit 5b — needs transport-stub infrastructure for tests). Browser hand-test required after UI surfaces land (covers EnvelopeCard + Regenerate + bulk buttons + initial-seed-from-DB + audit-history view in AdjustPanel).
- Phase 4 of the design doc — Planning Snapshot Review Before Drafting. **Commit 1 (computePlanningSnapshotHash tracer-bullet — pure helper, deterministic over world+characters+spine+outlines, version byte for future schema bumps) DONE 2026-05-04.** Remaining sub-items: commit 2 (planning_snapshots table + idempotent persistence), commit 3 (lock-snapshot route + GET /current returning live hash + mechanical-health audit), commit 4 (UI panel with collapsible artifact-slice groups + mechanical-health badges + Lock button), commit 5 (replay-on-stale enforcement at draft start: read locked hash → recompute → refuse-to-draft on drift).
- Phase 5 of the design doc — Editorial Proposal Workbench (`EditorialFlagProposal` + `ProseEditProposal` schemas; one LLM tracer-bullet module).
- Phase 6 of the design doc — Approval Policy Engine (deterministic policy evaluator over the unified proposal path).
- L73 / Continuity candidate provisional; opens with explicit charter when user picks the next direction. Architecture question on the table re: dynamic-state vs tagged-context — see 2026-05-03 retro.
- Human-confirm Step 2C planner semantic queue from `docs/artifacts/planner-semantic-labeling-novel-1777786463873-2026-05-03T221815312Z.json` only if/when planner direct auto-commit is reopened; the proposal-flow direction (now Phase 1 cleared) supersedes the auto-commit path for collaborative ideation. Compute live generated-ID precision/recall/F1 before any direct planner Canon write is reauthorized.

## Completed

- 2026-05-04 Phase 4 commit 1 — computePlanningSnapshotHash tracer-bullet. Pure helper at `src/canon/planning-snapshot.ts` hashing (world, characters, spine, outlines) via `stableHash` (canonical-JSON sha256). Determinism comes from accessor orderings (`getCharacters` ORDER BY id; `getChapterOutlines` ORDER BY chapter_number) and `stableHash`'s recursive key sort. Two entrypoints: pure (already-loaded inputs) and DB-bound (reads via existing accessors with safe fallbacks for missing world/spine/outlines). Explicit `version: "v1"` byte mixed into digest so future schema bumps (e.g., adding planning directives) don't silently invalidate pinned hashes. Pending canon proposals deliberately NOT in v1 input set (no-ghost-canon: pending shouldn't move the snapshot, approved/rejected already affect outline state). 10/10 tests pass / 11 expects (determinism, JSON round-trip, per-input sensitivity, v1↔v2 namespace, fresh-novel case, key-order independence). Out of scope (deferred to Phase 4 commits 2-5): persistence, lock route, UI panel, replay-on-stale enforcement at draft start. Diff scope: 2 files (+235; new module + colocated test). Server tsc clean. Exp #445.

- 2026-05-04 Phase 3 commit 4 follow-up D — audit-history view in AdjustPanel. Adds a collapsible "Show audit history" toggle below the active envelope batch. Opening fetches `GET /proposal-envelopes?status=all` and renders non-pending rows (approved/rejected/modified) with status badge color-coded by semantic, patch summary, resolved-at timestamp, and parentEnvelopeId reference (last 16 chars in monospace, where present — the regen lineage from follow-up B is now user-observable). Read-only — no Approve/Reject buttons. Pending rows filtered out (already in active batch above; duplication would be noisy). Re-fetches on novelId change when open. Refresh button available. Closes the last commit-4 follow-up; 4 of 4 closed (A/B/C/D). Phase 3 remaining is just commit 5b (LLM-firing quick actions, parked behind transport-stub infrastructure). Diff scope: 1 file (+173). tsc + vite clean (524.12 kB / 156.52 kB gzip; +3.02 kB / +0.74 kB gzip vs prior). Browser-untested per CLAUDE.md UI rule. Exp #444.

- 2026-05-04 Phase 3 commit 4 follow-up C — UI consumer of persisted envelopes (load pending on AdjustPanel mount). On `AdjustPanel` mount (and when novelId changes), fetch `GET /proposal-envelopes?status=pending` and seed `envelopes` + `envelopeStates`. Closes the cross-session-resumability gap that motivated commit 4 in the first place — prior-session pending proposals now show up immediately. Best-effort fetch (404/500/network → log + empty panel; operator still drives a fresh /adjust turn). Defense against clobbering: only seeds when panel is in initial empty state. New `listProposalEnvelopes(novelId, { status?, limit? })` API client. Out of scope for this commit (deferred to follow-up D): audit-history view (status="all" + status badges + resolved-at column + parentEnvelopeId chain rendering). MVP scope chosen so the resumability fix ships before tunneling on audit-view UX. Diff scope: 2 files (+50/-1; ui/src/api.ts +18, ui/src/components/ArtifactPreviews.tsx +32/-1). tsc + vite clean (521.10 kB / 155.78 kB gzip; +0.62 kB / +0.18 kB gzip vs prior). Browser-untested per CLAUDE.md UI rule. Exp #443.

- 2026-05-04 Phase 3 commit 4 follow-up B — parentEnvelopeId provenance on regen. When the operator hits Regenerate on a stale envelope, the new envelope batch carries the stale envelope's id as `source.parentEnvelopeId`, persisted to `proposal_envelopes.parent_envelope_id` (column reserved in commit 4 7f41833). Plumbing: `regenerateFromEnvelope(env)` → `sendMessage(msg, { parentEnvelopeId: env.id })` → `adjustNovel(novelId, msg, turns, opts)` → `POST /adjust` body field → `buildArtifactPatchEnvelope({ ..., parentEnvelopeId })` → `envelope.source.parentEnvelopeId` → existing persistence. Deliberately NOT in the deterministic id seed — lineage is provenance metadata, not identity; `ON CONFLICT (id) DO NOTHING` preserves the original row when regen lands on identical patch+target. Pinned by `parentEnvelopeId does NOT affect envelope id` test. 27/27 envelope unit + persistence pass / 101 expects (was 23/87, +4 tests / +14 expects); 27/27 resolve-route still passes / 121 expects. tsc + vite clean (520.48 kB / 155.60 kB gzip; +0.12 kB / +0.04 kB gzip vs prior). Browser-untested per CLAUDE.md UI rule. Exp #442.

- 2026-05-04 Phase 3 commit 4 follow-up A — resolve-route persistence wiring. The `/proposal-envelopes/resolve` route now writes the envelope's resolution status to `proposal_envelopes` *inside the same `db.begin(...)` transaction* as the artifact apply. The artifact and audit-trail row commit (or roll back) together — a stale-precondition rollback leaves the envelope row 'pending' so the operator can retry. New 409 outcome `alreadyResolved` + `actualStatus` catches duplicate REJECT races (which the artifact-hash precondition cannot — rejects don't move the hash); approved+approved races stay caught by the existing hash precondition. Body-carry semantics preserved: when the envelope row is missing (e.g., /adjust DB-write failed), the route logs an audit-gap warning and the artifact apply still happens. Forward-compatible `actualStatus?: string` field on `ResolveProposalEnvelopeResponse` so the UI can surface it later. 27/27 envelope-route tests pass / 121 expects (six new persistence tests + 21 pre-existing). Server tsc clean; UI tsc + vite build clean (520.36 kB / 155.56 kB gzip — additive type field only). Pre-existing canon-proposal-routes.test.ts flakes confirmed unchanged via git-stash counterfactual. Diff scope: 3 files (`src/orchestrator/proposal-envelope-routes.ts` +72/-18; `src/orchestrator/proposal-envelope-routes.test.ts` +196/-2; `ui/src/api.ts` +2). Exp #441.

- 2026-05-04 Phase 3 commit 4 — persistence (proposal_envelopes table + insert + list). New `sql/037_proposal_envelopes.sql` migration creates a generic envelope table indexed on (novel_id, status, created_at). New `src/db/proposal-envelopes.ts` ships typed helpers for `artifact_patch` envelopes: `insertArtifactPatchEnvelope` (idempotent `ON CONFLICT (id) DO NOTHING` — safe to retry on transient pool errors), `listArtifactPatchEnvelopes` (default pending; `status: "all"` for audit), `findEnvelopeById`, `updateEnvelopeResolution` (pending → resolved with `WHERE status='pending'` guard mirroring canon_proposals), `deleteEnvelopesForNovel`. `/adjust` route now persists each returned envelope best-effort (logs + continues on DB failure; UI still gets the envelope in the response so body-carry resolve still works). New `GET /api/novel/:id/proposal-envelopes` route with `?status=` and `?limit=` (capped 500). 7/7 tests pass / 32 expects. Out of scope for this commit: UI consumer of persisted envelopes (load on session open, history view), resolve-route wiring to load by id + persist resolution status, parentEnvelopeId provenance on regen (schema column reserved). Server tsc clean. Exp #439.

- 2026-05-04 Phase 3 commit 5a — bulk quick actions on AdjustPanel envelope cards. Two new buttons above the EnvelopeCard list: `Approve all low-risk (N)` (loops `resolveProposalEnvelope` on every PENDING envelope where `risk === "low"`) and `Reject all (N)` (rejects every PENDING envelope). Counts reflect PENDING-only — busy/done/stale/error cards skipped so prior decisions stand. window.confirm() before firing; sequential dispatch (each call is its own atomic compare-and-apply transaction, so concurrency safety is preserved). Stale-precondition mid-bulk surfaces normally on the affected card; bulk continues. The LLM-firing quick actions (ask-alternatives, explain-patch) defer to commit 5b because they need transport-stub infrastructure for tests. tsc clean; vite build clean (520.36 kB / 155.56 kB gzip; +1.38 kB / +0.31 kB gzip vs prior). Browser-untested per CLAUDE.md UI rule. Exp #438.

- 2026-05-04 Phase 3 commit 3 — regenerate-on-stale UI affordance. EnvelopeCard's stale state now renders a `Regenerate` button alongside the version-diff line; clicking re-fires `/adjust` with the original `envelope.source.userMessage` so the model re-derives the patch given the latest artifact state. UI-only — same model task, same system prompt, just refreshed state. No new server route. `AdjustPanel.send` was split into `sendMessage(msg)` (programmatic) + `send()` (input-driven wrapper) so regen reuses the existing machinery. Defensive guard surfaces an error state when `envelope.source.userMessage` is missing instead of firing a no-op regen. Trade-offs documented in source: approved/rejected envelopes from the prior batch are discarded on regen (state machine resets); no parentEnvelopeId provenance (deferred to commit 4 persistence). tsc clean; vite build clean (518.98 kB / 155.25 kB gzip; +0.89 kB / +0.30 kB gzip vs prior). Browser-untested per CLAUDE.md UI rule. Exp #437.

- 2026-05-04 Phase 3 commit 2.5 — UI consumer of /proposal-envelopes/resolve. New `resolveProposalEnvelope` API client in `ui/src/api.ts` and new `EnvelopeCard` component in `ArtifactPreviews.tsx` (per-envelope Approve/Reject buttons in the AdjustPanel). Each envelope card carries risk badge (low/medium/high/mechanical color-coded), summarized payload, and a per-row state machine (pending → busy → done | stale | error). Stale-precondition surfaces the version diff inline ("expected v abc12345 → now v def67890") with a placeholder "Regenerate (commit 3) coming soon" hint; commit 3 will wire the regen action. Legacy `Apply all` path preserved as fallback when older servers don't return `proposalEnvelopes` (Phase 3 commit 1 was additive). Modify deferred — kind-aware editor is the same gate as Codex round-3 HIGH for canon-proposals (artifact-patch envelopes are structured by nature). tsc clean; vite build clean (518.09 kB / 154.95 kB gzip; +3.56 kB / +0.97 kB gzip vs prior). Browser-untested per CLAUDE.md UI rule. Exp #436.

- 2026-05-04 Codex round-4 fix bundle (03af3e2 / b5aa3d4 / 1ab2e4d) — three commits closing all 1 HIGH + 2 MEDIUM from the round-4 review of 1e550ad^..9fb7cd5. **HIGH** (atomic compare-and-apply, b5aa3d4 exp #434): the resolve route's hash precondition was non-binding — read-then-apply across the bare `db` connection meant a concurrent edit between the read and the apply could clobber stale data. Fix: wrap precondition + apply in a single `db.begin(...)`; SELECT FOR UPDATE locks the target row inside the tx; recompute hash from locked content; mismatch throws a wrapped outcome that unwinds the transaction with no writes. New race-window test: `Promise.all` two concurrent same-target resolves → sorted statuses `[200, 409]`, exactly one apply, winner's payload in DB. **MEDIUM 2** (rename atomicity, 03af3e2 exp #433): `updateCharacterFields` rename ran 3 statements (character UPDATE + 2 relationship_states rewrites) on the bare `db` connection — partial failures left `characters.name` and `relationship_states` inconsistent. Fix: wrap internal work in `db.begin(...)` when called bare; add optional `executor` parameter to all 3 update helpers (characters/world/spine) so HIGH can thread its tx through. New `src/db/world.test.ts`: 3 pass / 10 expects (happy-path rename, non-rename leaves rels alone, outer-tx rollback rolls back all 3 writes). **MEDIUM 1** (error masquerade, 1ab2e4d exp #435): structurally closed by HIGH — the new `readLockedTarget` has no .catch() swallows so missing rows surface as 404 and real DB errors as 500. Two regression-pinning tests added (worldUpdate/spineUpdate missing → 404 not 500). Round-4 verdict was RED on these three; fix commits flip it to GREEN. Phase 3 commit 3 (regenerate on stale precondition) now unblocked. Envelope route suite 21/21 / 85 expects.

- 2026-05-04 Phase 3 commit 2 — per-patch resolve route + hash-precondition guard. New `src/orchestrator/proposal-envelope-routes.ts` exposes `POST /api/novel/:novelId/proposal-envelopes/resolve` with body `{envelope, status, modifiedPayload?, operatorNote?}`. Approves applies `envelope.payload`; rejects no-op; modifies apply `modifiedPayload` after asserting it targets the same artifact (cross-target/type modifies → 400). Live artifact hash recomputed via `stableHash(canonicalize(...))` (canonical-JSON contract from c307ddd) and compared to `envelope.target.currentVersion` — mismatch surfaces as 409 + `{expectedVersion, actualVersion}`. Closes the design's "stale patches cannot overwrite newer human edits" acceptance. Missing target → 404; novelId mismatch / invalid body / invalid status / malformed JSON → 400. Apply dispatches to existing `updateCharacterFields` / `updateWorldBibleFields` / `updateStorySpineFields`; returns post-apply hash. Persistence not in scope (commit 4); regenerate-on-stale not in scope (commit 3). 18/18 tests / 73 expects; canon suite 212/212; server tsc clean. Wired in `src/orchestrator/server.ts`. See `docs/decisions.md` §"Phase 3 commit 2 — per-patch resolve route". Exp #432.

- 2026-05-04 Codex round-3 fix bundle (ca9a6a8 / 7ba4569 / c307ddd) — three commits closing all 1 HIGH + 2 MEDIUM from the round-3 review of ee19011..1e550ad. **HIGH** (canon-integrity, ca9a6a8 exp #429): Modify… UI gated to `established_fact` via new `isModifySafeKind` helper; structured kinds (`knowledge_change` / `character_state` / `promise` / `payoff`) render disabled with explanatory tooltip + defense-in-depth `onSaveModify` guard. Closes the hole where editing only `text` while preserving stale `data` would commit a human-approved canon row with contradictory text vs. data. **MEDIUM 1** (audit-history, 7ba4569 exp #430): non-pending row renderer now reads `proposal.modifiedFact` as primary when `status === "modified"` and demotes `proposedFact.text` to a ghost-italic "original:" line. Operators can now inspect what actually entered canon for modify resolutions; pending/approved/rejected rows unchanged. **MEDIUM 2** (canonical hash, c307ddd exp #431): new `canonicalize` serializer (recursive key-sort + array-order preservation) feeds `stableHash`, making envelope `id` and `target.currentVersion` restart-stable across runtimes/JSON-roundtrips. 5 new envelope tests (16/16 pass / 55 expects); canon suite 212/212. Round-3 verdict was RED on these three; fix commits flip it to GREEN. Browser-untested per CLAUDE.md UI rule on the two UI fixes; logic-clean. Phase 3 commits 2-5 now unblocked on a sound envelope foundation.

- 2026-05-04 Codex round-1 LOW backlog cleared — three deferred defense-in-depth tests closed in two commits + one verification. (1) `6249ea7` — race-window deterministic test for concurrent same-proposal resolves: `Promise.all` two POST resolves on one pending row, assert sorted statuses `[200, 409]`, loser `actualStatus` = winner status, DB-side post-condition (single proposal row + canon row iff approved); test passes 1/1 + 12 expects in ~5.6s. (2) `1993e17` — schema-version drift guard test: generate v1 proposals (override) then v2 (default) → 60 disjoint pending rows, factsAsOfChapter still empty, approve v1+v2 of same source-item produces 2-version canon chain (v1.superseded_by_version = v2.version), both proposal rows persist as `approved`, re-running v2 generate is a 0-create no-op, 58 still pending; test passes 1/1 + 53 expects in ~10.4s. (3) Programmer-bug failure-path test (Codex MEDIUM 1) verified already covered by the existing `planner-canon-proposals.test.ts:537` test ("buildPlannerCanonProposals throw (programmer bug) propagates from autogen helper") — no new commit needed. Both new tests use 30s per-test timeout (LXC roundtrip latency). Exps #427 / #428.

- 2026-05-04 Phase 3 commit 1 — ReviewProposalEnvelope shape + additive adjuster wrap. New `src/canon/proposal-envelope.ts` defines the shared TypeScript projection per design doc §Proposal Envelope (id, kind, novelId, target, source, status, risk, summary, rationale, evidence, payload, precondition, policyRecommendation, createdAt) with `ArtifactPatchEnvelope = ReviewProposalEnvelope<AdjusterPatch>` specialization. `buildArtifactPatchEnvelope` produces deterministic ids (sha256 over patch + target version + index), risk classification (characterRename → medium, others → low), target refs (character / world_bible / story_spine), and artifact-hash preconditions. `/adjust` route now returns `proposalEnvelopes` alongside legacy `proposedPatches` (additive, non-breaking). UI client gains the envelope type + `AdjustResponse.proposalEnvelopes?` (optional). 10/10 unit tests pass / 42 expects. 206/206 canon tests pass. tsc clean; vite build clean (513.67 kB / 153.70 kB gzip — no size change, types are erased + field optional). Persistence + per-patch resolve routes deferred to Phase 3 commits 2-5. Exp #426.

- 2026-05-04 Codex round-2 fix bundle (a9757d4 / 245b9c7 / d913fb4) — three commits closing all 3 MEDIUMs from the round-2 review of b554e42…4fec19e. M3 (`cmdGenerate` checks `persistenceError` before `gateClear` for exit code; atomic rollback no longer prints `gate=CLEAR` exit 0). M2 (single-resolve `findProposal` + stale-status checks now inside the try/catch boundary; transient pre-read DB blip surfaces as structured 500/409 instead of escaping). M1 (`persistedChapterOutlineSchema` strict variant rejects rows missing audit-critical arrays or carrying corrupt beat obligations; permissive variant kept for LLM-ingest/round-trip). Round-2 verdict: 0 HIGH / 3 MEDIUM / 0 LOW; transactional core verified sound. New schema.test.ts (7/7 pass). Codex confidence shipped Phase 3-6 on this foundation: MEDIUM (concrete fixes recommended, all three now closed). Exps #423 / #424 / #425.

- 2026-05-04 UI modify-with-edits surface for canon-proposals page — pending rows gain a `Modify…` button next to Approve/Reject. Clicking expands the Proposed-fact column into an inline form: original text rendered as a ghost-italic quote above the editable textarea + confidence input (0-1, optional override) + free-form note. Save submits `resolveCanonProposal({ status: "modified", modifiedFact, operatorNote, expectedStatus: "pending" })`; id/kind/source stay fixed so the audit trail can reconstruct the original-vs-modified delta. Validation: text non-empty, confidence in [0,1] if set. Cancel collapses without a write. Only one row can be modified at a time (page-level state lift). tsc clean; vite build clean (513.67 kB / 153.70 kB gzip; +3.80 kB / +0.86 kB gzip vs status-tab baseline). Server modified-path tests still pass (2/2). Browser-untested per CLAUDE.md UI rule. See `docs/decisions.md` §"Canon proposal modify-with-edits UI surface". Exp #422.

- 2026-05-04 UI audit-history status tab for canon-proposals page — `pending | approved | rejected | modified | all` tab strip above the filter row, click applies immediately. `listCanonProposals` API client gains optional `status` arg (single | array | "all"; "pending" remains default to keep the URL clean for back-compat). Non-pending tabs hide bulk + per-row decision buttons (read-only audit view); rows show a status badge + ISO-formatted resolvedAt + operatorNote in the Decision column. Clear button preserves active status tab. tsc clean; vite build clean (509.87 kB / 152.84 kB gzip; +2.32 kB / +0.61 kB gzip vs bulk-action baseline). Server bulk-resolve flakes unchanged (UI-only diff). Browser-untested per CLAUDE.md UI rule. See `docs/decisions.md` §"Canon proposal audit-history status tab UI". Exp #421.

- 2026-05-04 UI bulk-action affordance for canon-proposals page — `Approve all (N)` / `Reject all (N)` buttons above the table when proposals are listed. Wraps the existing `bulkResolveCanonProposals` client (commit `032d8c0`). Acts on the visible set (post-filter), soft-cap 200/call mirrors the server cap, `window.confirm()` before fire, summary banner reports `ok/error` counts, optimistic remove on full success / reload-to-authoritative on partial errors. tsc clean; vite build clean (507.55 kB / 152.23 kB gzip). Server regression sweep 293/295 pass — 2 failures are pre-existing race-window flakes in the bulk-resolve test family (already queued as the LOW from Codex round-1; not introduced by this UI change). Browser-untested per CLAUDE.md UI rule. See `docs/decisions.md` §"Canon proposal bulk-action UI affordance". Exp #420.

- 2026-05-03 Codex round-1 fix bundle (commits b554e42 → 4fec19e). Six commits closing all HIGH (3) + all MEDIUM (4) + 2 LOW Codex findings: atomic insert batch + narrowed Phase 1.5 catch + persistenceError surface (b554e42); concurrent-resolve race → 409 + actualStatus via `isResolveConcurrencyConflict` predicate (d654065); bulk-resolve per-row resilience + all-error batch test (d8251d5); generate-from-outline outline validation → 422 (6aa4076); CLI mass-write guard (--force) + status enum hardening (50c7bee); character_state structured payload preservation + schema v2 (4fec19e). 293-test sweep / 1,615 expects; tsc clean; recall 0.927.
- 2026-05-03 canon-proposal operator CLI (`scripts/canon/proposals.ts`) — terminal-friendly path over the substrate. Subcommands: `list` / `approve` / `reject` / `approve-all` / `reject-all` / `generate`. Calls DB helpers + substrate directly (not the HTTP routes), works without an orchestrator. tsc clean; manual smoke (usage / `--status=bogus` / unknown-novel-list) verified. Commit `2c61d59`, exp 413.
- 2026-05-03 canon-proposal bulk-resolve endpoint — `POST /api/novel/:id/canon-proposals/bulk-resolve` with best-effort per-row resolution. 200-row soft cap. UI client `bulkResolveCanonProposals` added; UI affordance deferred. 6 new tests; 286-test sweep. Commit `032d8c0`, exp 412.
- 2026-05-03 canon-proposal list endpoint status filter — `?status=` query param (single | CSV | `all`); pending-only kept as default for back-compat. Uses explicit `{...}::text[]` literal because Bun's driver serializes JS arrays as comma-strings (Postgres rejects). 5 new tests; 280-test sweep. Commit `e83d1f5`, exp 411.
- docs/sessions/2026-05-03-collaborative-proposal-workflow-phase-2b.md — clean pass (a) browser-untested. **Phase 2B — Studio review panel UI.** New page `ui/src/components/CanonProposalsPage.tsx` at `/app/canon-proposals/:novelId` renders pending proposals + Approve/Reject + filters + Generate-from-outline. New API client `listCanonProposals` / `resolveCanonProposal` / `generateProposalsFromOutline` in `ui/src/api.ts`. Modify-with-edits not in v1. tsc clean; vite build clean (72 modules, 505.41 kB / 151.71 kB gzip); 275-test server sweep green; recall 0.927. Browser hand-test by operator pending.
- docs/sessions/2026-05-03-collaborative-proposal-workflow-phase-1-5-autowire.md — clean pass (a). **Phase 1.5 — auto-wire planner→proposals.** The planning phase now auto-fires `generatePlannerCanonProposals` after outlines are saved (`src/phases/planning.ts` calls `autogenPlannerProposalsAfterPlanning` helper). Idempotent on replanning; errors logged but non-blocking. 21 harness tests; 275-test full sweep green.
- docs/sessions/2026-05-03-collaborative-proposal-workflow-phase-2a-telemetry.md — clean pass (a). **Phase 2A telemetry — proposal lifecycle events.** `trace()` events (`canon-proposal-create`, `canon-proposal-resolve`, `canon-proposal-generate-summary`) wired into `generatePlannerCanonProposals` + `proposeCanonUpdate` + `resolveProposal`. 7 telemetry tests / 294 expects; 272-test full sweep green; tsc clean; recall 0.927.
- docs/sessions/2026-05-03-collaborative-proposal-workflow-phase-2a.md — clean pass (a). **Phase 2A — Canon Proposal Review API ships.** `src/orchestrator/canon-proposal-routes.ts` exposes list/resolve/generate-from-outline endpoints over `PostgresCanonSubstrate`. Stale-precondition (`expectedStatus`) returns 409 + `actualStatus`. 18 handler tests / 139 expects; 265-test canon+harness+orchestrator regression sweep green; tsc clean; recall 0.927.
- docs/sessions/2026-05-03-collaborative-proposal-workflow-phase-1.md — clean pass (a). **Phase 1 of the collaborative proposal workflow cleared.** New harness service `src/harness/planner-canon-proposals.ts` (pure `buildPlannerCanonProposals` + DB-writing `generatePlannerCanonProposals`) converts mechanically-valid planner source items into pending `CanonUpdateProposal` rows via `PostgresCanonSubstrate`. Deterministic id `planner:<novelId>:<sourceItemId>:<schemaVersion>` + new `insertProposalIfAbsent` (`ON CONFLICT (id) DO NOTHING`). 18-test harness suite (440 expects) green; 196/196 canon equivalence tests still green; tsc clean; recall 0.927.
- docs/sessions/2026-05-03-step-2c-live-planner-semantic-labeling.md — clean panel execution. DeepSeek V4 Flash/Pro overlap on `novel-1777786463873`: 30/30 source IDs, 128 calls, 126 valid, safety/verdict agreement 0.967, 28 review-free direct-write candidates, 26-row human-confirmation queue. Cost `$0.2756`, provider-reported cache ratio 0.987. Direct planner Canon writes remain blocked pending human confirmation + final precision/recall/F1.
- docs/sessions/2026-05-03-canon-substrate-postgres-adapter.md — clean pass (a). **Charter §1 (Canon Substrate) stop gate cleared** — production Postgres substrate (`sql/035`, `src/db/canon-substrate.ts`, `src/harness/canon-substrate.ts`) ships with the async-loader + sync-snapshot-wrapper pattern; adapter-equivalence suite (`src/canon/substrate-equivalence.test.ts`) runs the same 32-test behavioral spec against `InMemoryCanonSubstrate` and `PostgresCanonSubstrate` and passes against both. 178 canon tests pass, tsc clean, §0a recall still 0.927.
- exp #402 (L70b+L71+L72 stack validation A/B on `fantasy-archive`/`fantasy-debt`/`fantasy-system-heretic` ch1-2) — VALIDATED. Approval ch1: 1/3 → 3/3 across baseline → L70b → stack. debt FULL NOVEL APPROVED ($0.074). 3 settle invocations, 100% acceptance. Dominant blocker shifted to ch2 plan-check-exhausted on continuity + halluc-ungrounded (arch + heretic). $0.190 total. See `docs/decisions.md` §L70b+L71+L72 stack validation.
- docs/sessions/2026-05-03-L72-duplicate-sentence-punctuation-fp.md — SHIPPED unit-only (exp #401, Lever I-A). `normalizeSentence` was stripping all punctuation, so `"No."` and `"No?"` matched as duplicate-sentence; surfaced in debt ch2 att 1 of L70b A/B and triggered chapter regenerate cascade. Fix preserves `.?!`. 5 new tests, recall-preserving by construction.
- docs/sessions/2026-05-03-L71-chapter-plan-reviser-maxtokens.md — SHIPPED defensive (exp #400). chapter-plan-reviser maxTokens 6144 → 12288. Surfaced as 1/25 historical bail (exp #399 heretic ch1 finish_reason=length). Heretic retry ran clean but reviser never fired — defensive coverage for documented failure mode, escalation if 12288 also caps.
- docs/sessions/2026-05-03-L70b-per-fragment-targeted-rewrite.md — SHIPPED (exp #399, Lever I-D form (a)). Per-beat targeted rewrite via offset→beat mapping + `runSettleLoop` reuse, no writer-prompt change. 75% settle acceptance, +33pt approval (1/3 → 2/3 ch1), arch full novel approved. heretic regressed on plan-assist `reviser-rejected` (different surface where L70b code never ran; provably L70b-unattributable). Lesson: routing-only lanes need causal-attribution stop gates.
- docs/sessions/2026-05-02-L70-duplicate-fragment-paraphrase-ladder.md — REVERTED stop gate (b) (exp #398, Lever I-D form (b)). Prompt-only escalation; cross-surface coupling — heretic regressed approved → bailed plan-check (halluc `silver interlocking ring`); arch shifted to fused-boundary (`6 A.M.*`). Pivoted to L70b form (a). Three lessons captured in `docs/lessons-learned.md`.
- exp #392 (L65 live smoke on `fantasy-archive`) — chapter 1 approved att2 (L41/L63 verified live: 13/13 prompts carry AVOID INTEGRITY + paraphrase-one-side directives). Chapter 2 bailed at plan-check-exhausted on halluc-ungrounded "Senior Cataloguer". Critical phase finding: writer drift-invents fresh ungrounded entities each chapter-attempt ("Third Lamentation" → "Codex" → "Senior Cataloguer") rather than persisting — L65's carry-over architecture is correct but addresses persistence-mode, not drift-invention-mode. G-B priority elevated.
- docs/sessions/2026-05-02-L65-grounding-carryover.md — clean pass for chapter-attempt carry-over of LLM-confirmed ungrounded entities (exp #391, Lever G-A). Mirrors L41/L63 pattern; closes byte-identical-prose case from exp #389 (retroactive replay PASSES on `chapter_exhaustions` row for novel-1777768466618 ch1).
- docs/sessions/2026-05-02-grounding-phase-brief.md (phase brief) — 25% of plan-check-exhausted in 14 days cite halluc-ungrounded; identified Levers G-A / G-A2 / G-B / G-C with empirical sequencing.
- exp #389 (L63+L64 e2e on `fantasy-debt`) — L62/L63/L64 wires non-regressive. Chapter 1 reached integrity check with 0 fused-boundary/duplicate hits; integrity gate never had to fire. Smoke bailed at out-of-phase plan-check-exhausted (halluc-ungrounded "central spire"). Classifier: `new_blocker`. See L64 lane Results addendum.
- docs/sessions/2026-05-02-L64-integrity-exhaustion-gate.md — clean pass for routing final-attempt integrity exhaustion through `presentForExhaustion` with `kind: "integrity-exhausted"` (exp #388, Lever B; UI consumers in 826a6c1). Mirrors existing plan-check-exhausted dispatch; no SQL migration needed.
- docs/sessions/2026-05-02-L63-matched-pair-carryover.md — clean pass for matched-pair carry-over for duplicate-sentence + duplicate-fragment (exp #387, Lever A). Targets 72.9% of integrity-fail volume per phase brief. Empirical retry-replay validation deferred until a retry-bearing seed exercises the path.
- docs/sessions/2026-05-02-runner-archive-and-litrpg-validate.md (session) — runner archival + L62-validate clean pass (exp #386). Chapter 1 of fantasy-system-heretic approved with 0 fused-boundary issues from System UIDs. New blocker surfaced in continuity (out-of-phase; parked).
- docs/sessions/2026-05-02-integrity-retry-phase-brief.md (phase brief) — DB scan + code audit + L41 trace evidence; identified Levers A/B/C with empirical sequencing.
- docs/sessions/2026-05-02-L62-litrpg-integrity-guard.md — clean pass for LitRPG System-path identifier exemption in `detectFusedBoundaries` (commit `31e16a8`, exp #385). L63 candidate (chapter-attempt retry fall-through after second integrity failure) queued in `docs/todo.md`.
- docs/sessions/2026-05-02-L61-e2e-smoke-after-hardening.md — stop gate (b) new dominant blocker: prose-integrity false-positive on LitRPG System path identifiers + chapter-attempt retry escalates duplicate fragments. L62 candidate queued in `docs/todo.md`.
- docs/sessions/2026-05-02-L57-runner-review-gate.md — clean pass for review evidence before queued lane advance.
- docs/sessions/2026-05-02-L38-A-prior-context.md — refuted original missing-prior-context hypothesis.
- docs/sessions/2026-05-02-L38-F-reader-info-adherence.md — confirmed writer-side READER-INFO binding rule.
- docs/sessions/2026-05-02-L38-C-chapter-summary-wiring.md — closed chapter_summaries as obsolete.
- docs/sessions/2026-05-02-L38-G-intra-chapter-state.md — clean pass for same-chapter physical-state rule.
- docs/sessions/2026-05-02-L49-grounded-union-allowlist.md — clean pass for bounded title-strip grounded-union matching.
- docs/sessions/2026-05-02-L50-halluc-per-class-metrics.md — clean pass for halluc A/B per-class metrics.
- docs/sessions/2026-05-02-L51-halluc-structured-entity-metadata.md — clean pass for structured halluc A/B issue metadata.
- docs/sessions/2026-05-02-L52-checker-calibration-persist-default.md — clean pass for checker calibration rollups.
- docs/sessions/2026-05-02-L53-phase-eval-family-key.md — clean pass for phase-eval family-key comparability.
- docs/sessions/2026-05-02-L54-pre-loop-gate.md — clean pass for unattended pre-loop gate.
- docs/sessions/2026-05-02-L55-commit-range-docs-impact-audit.md — clean pass for commit-range docs-impact audit.
- docs/sessions/2026-05-02-L56-smoke-stop-classifier.md — clean pass for smoke-stop classifier.
