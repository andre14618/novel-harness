node_modules/
dist/
output/
backups/
data/harness.db
data/harness.db-shm
data/harness.db-wal
data/batches/
state/
.env
.DS_Store
novel.db
scripts/lora-data/
finetune-data/
wandb/
__pycache__/
*.pyc

# Ephemeral in-flight run registry — per-machine runtime state only
.claude/in-flight/

# Subagent worktrees — isolated per-session, never committed
.claude/worktrees/

# Scheduled-task state — per-machine runtime state only
.claude/scheduled_tasks.lock
