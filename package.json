{
  "name": "novel-harness",
  "type": "module",
  "private": true,
  "scripts": {
    "start": "bun src/index.ts",
    "dev": "bun --watch src/index.ts",
    "test": "bun test",
    "monitor": "bun scripts/agent/monitor.ts",
    "lane-runner": "bun scripts/agent/lane-runner.ts",
    "lane-message": "bun scripts/agent/lane-message.ts",
    "finalize-docs": "bun scripts/agent/finalize-docs.ts",
    "finalizer-packet": "bun scripts/agent/finalizer-packet.ts"
  },
  "dependencies": {
    "yaml": "^2.8.3",
    "zod": "^3.23"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^6.0.3"
  }
}
